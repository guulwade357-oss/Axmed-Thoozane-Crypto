import {cookies} from 'next/headers'; import {jwtVerify,SignJWT} from 'jose'; const key=()=>new TextEncoder().encode(process.env.JWT_SECRET||'CHANGE_ME'); export async function createSession(user:{id:string,role:string}){return new SignJWT(user).setProtectedHeader({alg:'HS256'}).setIssuedAt().setExpirationTime('7d').sign(key())} export async function session(){const t=(await cookies()).get('session')?.value;if(!t)return null;try{return (await jwtVerify(t,key())).payload as {id:string,role:string}}catch{return null}} export async function requireUser(){const s=await session();if(!s)throw new Error('UNAUTHORIZED');return s} export async function requireAdmin(){const s=await requireUser();if(s.role!=='ADMIN')throw new Error('FORBIDDEN');return s}
