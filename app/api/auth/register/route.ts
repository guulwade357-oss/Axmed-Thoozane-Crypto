import {NextResponse} from 'next/server'; import {db} from '@/lib/db'; import bcrypt from 'bcryptjs'; import {z} from 'zod'; import {createSession} from '@/lib/auth'; const schema=z.object({name:z.string().min(2),email:z.email(),password:z.string().min(8)}); export async function POST(req:Request){try{const b=schema.parse(await req.json());const email=b.email.toLowerCase();if(await db.user.findUnique({where:{email}}))return NextResponse.json({error:'Email already registered'},{status:409});const u=await db.user.create({data:{name:b.name,email,passwordHash:await bcrypt.hash(b.password,12)}});const token=await createSession({id:u.id,role:u.role});const r=NextResponse.json({ok:true});r.cookies.set('session',token,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:604800});return r}catch{return NextResponse.json({error:'Invalid registration data'},{status:400})}}
