'use client'; import {useState} from 'react'; import Link from 'next/link';
export default function Register(){const[msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),password:f.get('password')})});setMsg(r.ok?'Account created successfully. You can log in now.':(await r.json()).error||'Registration failed.')}return <main className="auth"><form onSubmit={submit}><Link className="brand" href="/"><span className="seal">AT</span><span>AXMED THOOZANE <b>CRYPTO</b></span></Link><h1>Create Student Account</h1><input name="name" placeholder="Full name" required/><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" minLength={8} placeholder="Password (8+ characters)" required/><button className="btn">Create Account</button><p>{msg}</p><Link href="/login">Already have an account?</Link></form></main>}
