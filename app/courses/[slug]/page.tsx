import Link from 'next/link';
import { notFound } from 'next/navigation';
import { db } from '@/lib/db';
export default async function CoursePage({params}:{params:{slug:string}}){const c=await db.course.findUnique({where:{slug:params.slug},include:{modules:{orderBy:{position:'asc'},include:{lessons:{orderBy:{position:'asc'}}}}}});if(!c||!c.published)notFound();const lessons=c.modules.reduce((n,m)=>n+m.lessons.length,0);return <main className="section"><div className="container"><Link href="/courses" className="eyebrow">← ALL COURSES</Link><div style={{maxWidth:850,marginTop:20}}><span className="badge">COURSE</span><h1 style={{fontFamily:'Space Grotesk',fontSize:'clamp(42px,6vw,72px)',margin:'16px 0'}}>{c.title}</h1><p className="section-intro">{c.description}</p><div className="meta" style={{marginTop:20}}><span>{c.modules.length} modules</span><span>{lessons} lessons</span><span>Quizzes & progress</span></div></div><div className="section" style={{padding:'50px 0 0',border:0}}><div className="eyebrow">CURRICULUM</div><h2>What you'll learn</h2><div className="courses" style={{marginTop:25}}>{c.modules.map(m=><article className="course" key={m.id}><div className="course-top"><h3>{m.title}</h3><span className="badge">{m.lessons.length} lessons</span></div><div style={{display:'grid',gap:8,marginTop:15}}>{m.lessons.map(l=><Link key={l.id} href={`/courses/${c.slug}/lessons/${l.slug}`} className="resource">{l.position}. {l.title}<span>Open lesson →</span></Link>)}</div></article>)}</div></div></div></main>}
