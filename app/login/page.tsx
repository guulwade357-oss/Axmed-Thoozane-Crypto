'use client';
import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function Login(){const[msg,setMsg]=useState('');const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMsg('Signing in…');const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const d=await r.json();if(r.ok){router.push(d.role==='ADMIN'?'/admin':'/dashboard');router.refresh()}else setMsg(d.error||'Login failed.')}return <main className="auth"><div className="panel"><Link className="brand" href="/"><span className="logo">TC</span><span>THOOZANE CRYPTO<small>TRADER • ACADEMY</small></span></Link><h1>Student Login</h1><p className="section-intro">Enter your academy account to continue learning.</p><form className="form" onSubmit={submit}><label>Email<input name="email" type="email" placeholder="you@example.com" required/></label><label>Password<input name="password" type="password" placeholder="••••••••" minLength={8} required/></label><button className="btn btn-primary" type="submit">Sign in →</button>{msg&&<p style={{color:'var(--gold)'}}>{msg}</p>}</form><p style={{color:'var(--muted)',fontSize:14}}>New to the academy? <Link href="/register" style={{color:'var(--gold)'}}>Create an account</Link></p><Link href="/" style={{color:'var(--muted)',fontSize:13}}>← Back to website</Link></div></main>}
